/// Layout breakpoints used in the app.
///
/// NOTE: At the time of release, a liquid layout will be used instead of
/// a responsive layout.
/// So this breakpoint may not need to be used.
class Breakpoint {
  static const double desktop = 900;
  static const double tablet = 600;
}
